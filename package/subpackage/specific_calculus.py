from math import sqrt # to use math.sqrt() function

def power(base, exponent):
	print(f"Power result is: {base ** exponent}")

def round_num(num):
	print(f"Round result is: {round(num)}")

def sqrt_num(num):
	print(f"Square root result is: {sqrt(num)}")